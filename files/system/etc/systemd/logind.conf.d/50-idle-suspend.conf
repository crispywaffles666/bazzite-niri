# Tuigreet updates the TTY idle time when a user presses a key. Niri and
# Noctalia do not set logind's idle hint, so this rule runs only at the greeter.
# Noctalia handles idle suspend after login. Media playback can block both.
[Login]
IdleAction=suspend
IdleActionSec=15min
